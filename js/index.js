$(document).ready(function () {
  // Wait for the document to be ready

  // Select the button with the ID "myButton" and add a click event handler
  $("#myButton").click(function () {
    // Change the text of the paragraph with the ID "myParagraph"
    $("#myParagraph").text("Hello, jQuery!");
  });
  
});
